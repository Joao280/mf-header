apiVersion: v1
kind: Service
metadata:
  name: { { include "mf-header.fullname" . } }
  labels: { { - include "mf-header.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "mf-header.selectorLabels" . | nindent 4 } }
